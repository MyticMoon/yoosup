To install the application
1) install nodejs - npm
2) Install dependencies using npm
	router
	session-timeout

	** Can use meteorite to install dependencies instead of using npm (??)
3) meteorite
	install meteorite
	npm install -g meteorite
	usr/bin: ln -s nodejs node

	mrt add router

	-- or --

	mrt install

4) access mongodb by .meteor/tools/****/mongodb/bin/mongo --port 3001

5) One modal but pass different parameters to the modal